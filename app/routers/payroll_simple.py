"""DEPRECATED — replaced by payroll.py (full payroll router).

This temporary stub router was removed from main.py. It duplicated the
/api/payroll prefix with reduced functionality and an unauthenticated PDF
endpoint. Kept as an empty module only to avoid breaking stale imports;
do not add routes here.
"""
